#include <unistd.h>

void ft_putchar(char c)
{
	write (1, &c, 1);
}

int ft_atoi(char *str)
{
	int rst;
	int i;

	rst = 0;
	i = 0;
	while (str[i] != '\0' && (	str[i] == ' ' ||   )

	return (rst);
}

int main(void)
{
	return (0);
}
